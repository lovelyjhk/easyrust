pub fn var_2_1() {
    // 좋은 변수명은 네이밍 가이드라인을 따릅니다.
    let english_word = "hello";
    let number_of_students = 20;
    let maximum_temperature = 35.5;
    let user_input = "Rust Programming";
    let total_sales = 100_000;
    let is_active = true;
    let error_message = "File not found";
    let file_path = "/path/to/file.txt";
    let http_status_code = 200;

    // 예약어 사용을 피합니다.
    // let match = 10; // 'match' 예약어 사용 피하기

    // 변수명에 snake_case를 사용합니다.
    let number_of_books = 50;
    let user_age = 30;
    let product_price = 10.99;
    let is_valid_input = true;
    let error_code = 404;

    // 의미 있는 설명적인 이름을 사용합니다.
    let total_sales_in_year = 500_000;
    let user_name = "John Doe";
    let temperature_in_celsius = 25.5;
    let is_logged_in = false;
    let file_size_in_bytes = 1024;

    // 너무 긴 이름을 피하되 의미는 유지합니다.
    let total_number_of_registered_users = 1000; // registered_users_count 선호
    let result_of_multiplication_operation = 50 * 20; // multiplication_result 선호

    // 코드 스타일을 일관성 있게 유지합니다.
    let user_input_value = "Hello, world!";
    let maximum_value = 100;
    let is_enabled_flag = true;
    let error_occurred = false;
}
